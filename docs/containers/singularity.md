# Singularity / Apptainer

!!! note "TODO"
    Document: Converting Docker to SIF, singularity pull/build/exec, definition files, overlay filesystems, common pitfalls (user namespaces, writable dirs).
